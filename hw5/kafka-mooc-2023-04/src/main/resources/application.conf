akka {
  kafka {
    consumer {
      kafka-clients {
        enable.auto.commit = true
        bootstrap.servers = "localhost:19092"
        group.id = "group1"
        auto.offset.reset = "earliest"
      }
    }
  }
}